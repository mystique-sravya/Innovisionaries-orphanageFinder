import { Parser } from './';

declare const parser: {
    parsers: {
        glimmer: Parser;
    };
};
export = parseimport { Parser } from './';

declare const parser: {
    parsers: {
        glimmer: Parser;
    };
};
export = parser;
