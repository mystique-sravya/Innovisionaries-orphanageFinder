// @flow
import getUAString from '../utils/userAgent';

export default function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAStrin// @flow
import getUAString from '../utils/userAgent';

export default function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}
